<UserControl x:Class="CodeLibrary.Views.CodeInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="100*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Background="Gainsboro">
            <Label Content="{Binding CodeTitle}" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"></Label>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Label Content="{Binding CodeInfo}" FontSize="16"  VerticalAlignment="Center" HorizontalAlignment="Left"></Label>
        </StackPanel>
        <xctk:RichTextBox x:Name="richTextBox" FontSize="18" Grid.Row="2" Text="{Binding Data}" Background="Gainsboro" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"></xctk:RichTextBox>
    </Grid>
</UserControl>
