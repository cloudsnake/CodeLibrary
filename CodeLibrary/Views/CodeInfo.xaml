<UserControl x:Class="CodeLibrary.Views.CodeInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Background="Gray">
            <Label Content="{Binding CodeTitle}" HorizontalAlignment="Left"></Label>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Label Content="{Binding CodeInfo}" HorizontalAlignment="Left"></Label>
        </StackPanel>
        <xctk:RichTextBox x:Name="richTextBox" Grid.Row="2" Text="{Binding Data}" Background="Gray"></xctk:RichTextBox>
    </Grid>
</UserControl>
